---
phase: 08-provider-policies-catalog
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - docs/providers/google.md
  - docs/providers/groq.md
  - docs/providers/openrouter.md
  - docs/providers/mistral.md
  - docs/providers/huggingface.md
  - docs/providers/cerebras.md
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04, PROV-05, PROV-06, DX-02]

must_haves:
  truths:
    - 'Developer can follow docs to get a Google AI Studio API key from scratch'
    - 'Developer can follow docs to get a Groq API key from scratch'
    - 'Developer can follow docs to understand OpenRouter as a meta-provider and get an API key'
    - 'Developer can follow docs to get a Mistral API key from scratch'
    - 'Developer can follow docs to get a HuggingFace API key from scratch'
    - 'Developer can follow docs to get a Cerebras API key from scratch'
    - 'Each doc includes a working pennyllm config snippet using builder helpers'
  artifacts:
    - path: 'docs/providers/google.md'
      provides: 'Google AI Studio key acquisition and config reference'
      min_lines: 40
    - path: 'docs/providers/groq.md'
      provides: 'Groq key acquisition and config reference'
      min_lines: 40
    - path: 'docs/providers/openrouter.md'
      provides: 'OpenRouter key acquisition, meta-provider explainer, and config reference'
      min_lines: 50
    - path: 'docs/providers/mistral.md'
      provides: 'Mistral key acquisition and config reference'
      min_lines: 40
    - path: 'docs/providers/huggingface.md'
      provides: 'HuggingFace key acquisition and config reference'
      min_lines: 40
    - path: 'docs/providers/cerebras.md'
      provides: 'Cerebras key acquisition and config reference'
      min_lines: 40
  key_links: []
---

<objective>
Write key acquisition documentation for the first 6 providers: Google AI Studio, Groq, OpenRouter, Mistral, HuggingFace, and Cerebras.

Purpose: Developers need clear, self-contained guides to obtain free tier API keys and configure them with pennyllm. These docs are the primary reference since shipped defaults are no longer active routing data.

Output: 6 markdown files in `docs/providers/` with sign-up instructions, config snippets, and practical tips.
</objective>

<execution_context>
@/Users/muhammaduzair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhammaduzair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-provider-policies-catalog/08-CONTEXT.md
@.planning/phases/08-provider-policies-catalog/08-RESEARCH.md
@.planning/phases/08-provider-policies-catalog/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write provider docs for Google, Groq, and OpenRouter</name>
  <files>
    docs/providers/google.md
    docs/providers/groq.md
    docs/providers/openrouter.md
  </files>
  <action>
Create the `docs/providers/` directory if it doesn't exist. Write 3 provider documentation files following this exact template structure for each. All data comes from 08-RESEARCH.md provider research sections.

**Template structure for each provider doc:**

````markdown
# {Provider Name}

> Last verified: 2026-03-14 | [Official limits page]({source_url})
> Verify current limits against official docs before configuring.

## Quick Reference

| Property             | Value                                         |
| -------------------- | --------------------------------------------- |
| Tier type            | {recurring / trial credits / monthly credits} |
| Credit card required | {Yes / No}                                    |
| Sign-up URL          | {url}                                         |
| API key page         | {url}                                         |
| Env variable         | `{ENV_VAR}`                                   |
| AI SDK package       | `{package}`                                   |

## Getting Your API Key

1. {Step-by-step instructions specific to this provider}
2. ...
3. ...

## Free Tier Summary

{Table of limits from RESEARCH.md -- these are reference values, NOT hard-coded routing data}

## Configuration

```typescript
import { createRouter } from 'pennyllm';
import { createTokenLimit, createRateLimit, createCallLimit } from 'pennyllm/policy';
import type { {Provider}ProviderConfig } from 'pennyllm/types';

const {provider}: {Provider}ProviderConfig = {
  keys: [process.env.{ENV_VAR}!],
  limits: [
    // Adjust these values based on your account's current limits.
    // Check {limits_page_url} for the latest.
    createRateLimit({rpm}, 'per-minute'),
    createRateLimit({rpd}, 'daily'),
    createTokenLimit({tpm}, 'per-minute'),
  ],
};

const router = await createRouter({
  providers: { {provider_id}: {provider} },
});
```
````

## Gotchas & Tips

- {Provider-specific gotchas from RESEARCH.md}

## Paid Tier

{Brief mention of paid tier for comparison, from RESEARCH.md}

```

**Google-specific notes:**
- Include per-model limit table (Gemini 2.5 Pro, 2.5 Flash, 2.5 Flash-Lite, 2.0 Flash)
- Note limits are per Google Cloud project, not per API key
- Note Dec 2025 quota reduction
- Note known Gemini 2.5 Flash thinking mode issue

**Groq-specific notes:**
- Include per-model limit table for popular models
- Note limits are per-organization, not per-key
- Note that TPM limits are relatively low (6K-30K)
- Note ultra-fast inference speed

**OpenRouter-specific notes:**
- Add a "How OpenRouter Works" section explaining the meta-provider concept (CONTEXT.md locked decision)
- Explain `:free` suffix for free models
- Include top 5 recommended free models table
- Note the $10 credits purchased RPD unlock (50 -> 1000 RPD)
- Note that negative balance blocks ALL requests including free models
- Use `@ai-sdk/openai-compatible` with base URL `https://openrouter.ai/api/v1`

Config snippets MUST use placeholder-style comments like "Adjust these values based on your account's current limits" rather than asserting the values ARE the current limits. The values in the snippet are illustrative starting points from research.
  </action>
  <verify>
    <automated>ls -la /Users/muhammaduzair/Documents/github/experiments/pennyllm/pennyllm/docs/providers/google.md /Users/muhammaduzair/Documents/github/experiments/pennyllm/pennyllm/docs/providers/groq.md /Users/muhammaduzair/Documents/github/experiments/pennyllm/pennyllm/docs/providers/openrouter.md && wc -l /Users/muhammaduzair/Documents/github/experiments/pennyllm/pennyllm/docs/providers/*.md</automated>
  </verify>
  <done>
- `docs/providers/google.md` exists with key acquisition steps, per-model limits table, config snippet, gotchas
- `docs/providers/groq.md` exists with key acquisition steps, per-model limits table, config snippet, gotchas
- `docs/providers/openrouter.md` exists with meta-provider explainer, top 5 free models, config snippet, gotchas
- All three docs have `Last verified` date and link to official limits page
- Config snippets use builder helpers from Plan 01
  </done>
</task>

<task type="auto">
  <name>Task 2: Write provider docs for Mistral, HuggingFace, and Cerebras</name>
  <files>
    docs/providers/mistral.md
    docs/providers/huggingface.md
    docs/providers/cerebras.md
  </files>
  <action>
Write 3 more provider documentation files following the same template structure from Task 1. All data comes from 08-RESEARCH.md provider research sections.

**Mistral-specific notes:**
- Note "Experiment" plan name for free tier
- Note phone verification required for signup
- Note very low RPM (~2) which limits throughput
- Note 1B monthly token allowance is generous but RPM bottleneck
- Note specific per-model limits only viewable in admin dashboard
- Limits page: https://admin.mistral.ai/plateforme/limits

**HuggingFace-specific notes:**
- Explain credit-based billing ($0.10/month free)
- Note compute-time-based billing (not straightforward token counting)
- Note `HF_TOKEN` is native HF convention but AI SDK uses `HUGGINGFACE_API_KEY`
- Note HuggingFace is an aggregator/proxy (routes to multiple inference providers)
- Note PRO plan ($9/month) gives 20x more credits
- Config snippet should use `createCallLimit` since billing is request/compute-based, not token-based
- Base URL: `https://router.huggingface.co/v1`

**Cerebras-specific notes:**
- Include per-model limit table (llama3.1-8b, gpt-oss-120b, qwen-3-235b)
- Note free tier context window limited to 8,192 tokens
- Note ultra-fast inference speed (~2,600 tokens/sec)
- Note token bucket algorithm (continuous replenishment, not fixed reset)
- Config snippet should reflect TPD and RPM limits

Config snippets MUST use placeholder-style comments referencing official docs for current values.
  </action>
  <verify>
    <automated>ls -la /Users/muhammaduzair/Documents/github/experiments/pennyllm/pennyllm/docs/providers/mistral.md /Users/muhammaduzair/Documents/github/experiments/pennyllm/pennyllm/docs/providers/huggingface.md /Users/muhammaduzair/Documents/github/experiments/pennyllm/pennyllm/docs/providers/cerebras.md && wc -l /Users/muhammaduzair/Documents/github/experiments/pennyllm/pennyllm/docs/providers/*.md</automated>
  </verify>
  <done>
- `docs/providers/mistral.md` exists with Experiment plan details, phone verification note, RPM limitation
- `docs/providers/huggingface.md` exists with credit-based billing explanation, compute-time note
- `docs/providers/cerebras.md` exists with per-model limits, context window limitation, token bucket note
- All three docs have `Last verified` date and link to official limits page
- Config snippets use builder helpers from Plan 01
  </done>
</task>

</tasks>

<verification>
1. All 6 docs exist in `docs/providers/`: google.md, groq.md, openrouter.md, mistral.md, huggingface.md, cerebras.md
2. Each doc contains: Quick Reference table, Getting Your API Key section, Free Tier Summary, Configuration snippet, Gotchas & Tips
3. OpenRouter doc has "How OpenRouter Works" explainer section
4. Each config snippet imports from `pennyllm/policy` (builder helpers) and `pennyllm/types` (typed configs)
5. No hard-coded limit values presented as routing truth -- all docs say "verify against official docs"
</verification>

<success_criteria>
- 6 self-contained provider docs written with accurate key acquisition steps
- Each doc has working config snippet using builder helpers and typed provider configs
- OpenRouter includes meta-provider explainer with recommended free models
- All docs include lastVerified date, source URLs, and "verify before using" caveat
- Docs are self-contained (no links to external AI SDK docs that go stale)
</success_criteria>

<output>
After completion, create `.planning/phases/08-provider-policies-catalog/08-02-SUMMARY.md`
</output>
```
