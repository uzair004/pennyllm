---
phase: 05-model-catalog-selection
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/catalog/DefaultModelCatalog.test.ts
  - src/selection/strategies/round-robin.test.ts
  - src/selection/strategies/least-used.test.ts
  - src/selection/KeySelector.test.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CAT-04
  - CAT-05
  - ALGO-01
  - ALGO-02
  - ALGO-03
  - ALGO-04
  - ALGO-05

must_haves:
  truths:
    - 'Test scaffold files exist for all Phase 5 behavioral verification'
    - 'Each test file has pending/skipped test cases describing expected behaviors'
    - 'Tests import from module paths that Plan 01 will create (type stubs are sufficient)'
  artifacts:
    - path: 'src/catalog/DefaultModelCatalog.test.ts'
      provides: 'Test scaffold for catalog behaviors: refresh, capabilities, quality tiers, pricing, offline fallback'
      contains: 'describe'
    - path: 'src/selection/strategies/round-robin.test.ts'
      provides: 'Test scaffold for round-robin even distribution'
      contains: 'describe'
    - path: 'src/selection/strategies/least-used.test.ts'
      provides: 'Test scaffold for least-used quota selection'
      contains: 'describe'
    - path: 'src/selection/KeySelector.test.ts'
      provides: 'Test scaffold for KeySelector: strategy override, skip ineligible, custom strategy'
      contains: 'describe'
  key_links: []
---

<objective>
Create test scaffold files with pending test cases for all Phase 5 behaviors. These tests describe expected behavior upfront so implementation plans (02, 03) can validate against them.

Purpose: Satisfy Nyquist compliance. Every `<verify>` in Plans 02 and 03 references specific test files that must exist before those plans execute. This Wave 0 plan creates them with `it.todo()` or `it.skip()` placeholders.

Output: 4 test files with pending test cases covering CAT-01 through CAT-05 and ALGO-01 through ALGO-05.
</objective>

<execution_context>
@/Users/muhammaduzair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhammaduzair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-model-catalog-selection/05-VALIDATION.md

@src/storage/MemoryStorage.test.ts

<interfaces>
<!-- Test files reference types that Plan 01 will create. Use it.todo() for cases
     that cannot compile yet. The scaffolds establish WHAT to test, not HOW. -->

Expected type imports (created by Plan 01):

From src/types/interfaces.ts:

```typescript
export interface ModelCatalog {
  getModel(modelId: string): Promise<ModelMetadata | null>;
  listModels(filter?: ModelListFilter): Promise<ModelMetadata[]>;
  getCapabilities(modelId: string): Promise<ModelMetadata['capabilities'] | null>;
  refresh(): Promise<void>;
  close(): Promise<void>;
}

export interface SelectionStrategy {
  name: string;
  selectKey(context: SelectionContext): Promise<SelectionResult>;
}
```

From src/selection/types.ts:

```typescript
export interface SelectionContext {
  provider: string;
  model?: string;
  candidates: CandidateKey[];
  estimatedTokens?: number;
}

export interface CandidateKey {
  keyIndex: number;
  label?: string;
  eligible: boolean;
  cooldown: { until: string; reason: string } | null;
  evaluation: EvaluationResult;
}

export interface SelectionResult {
  keyIndex: number;
  reason: string;
}
```

From src/constants/index.ts:

```typescript
export const Strategy = {
  PRIORITY: 'priority',
  ROUND_ROBIN: 'round-robin',
  LEAST_USED: 'least-used',
} as const;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create catalog and selection test scaffolds with pending cases</name>
  <files>
    src/catalog/DefaultModelCatalog.test.ts,
    src/selection/strategies/round-robin.test.ts,
    src/selection/strategies/least-used.test.ts,
    src/selection/KeySelector.test.ts
  </files>
  <action>
Create 4 test scaffold files. Use `it.todo()` for all test cases since the implementation classes do not exist yet. Each file follows the Vitest pattern established in `src/storage/MemoryStorage.test.ts`.

**IMPORTANT per CLAUDE.md:** These are minimal scaffolds with `it.todo()` placeholders only. No actual test logic. Implementation plans (02, 03) will fill them in as needed during execution.

**1. Create src/catalog/DefaultModelCatalog.test.ts:**

```typescript
import { describe, it } from 'vitest';

describe('DefaultModelCatalog', () => {
  describe('refresh', () => {
    it.todo('fetches from models.dev API on first access');
    it.todo('supplements with OpenRouter for missing models');
    it.todo('validates API entries with Zod safeParse and skips invalid');
    it.todo('uses 5s timeout with no retry');
    it.todo('deduplicates concurrent refresh calls (single inflight)');
    it.todo('emits catalog:refreshed event with source and diff counts');
  });

  describe('capabilities', () => {
    it.todo('returns capability flags for known model');
    it.todo('returns null for unknown model');
    it.todo('supports 4 capability flags: reasoning, toolCall, structuredOutput, vision');
  });

  describe('quality-tiers', () => {
    it.todo('assigns quality tiers from static snapshot data');
    it.todo('supports frontier, high, mid, small tiers');
  });

  describe('pricing', () => {
    it.todo('normalizes pricing to per-1M-tokens format');
    it.todo('free models have zero pricing');
    it.todo('pricing has separate promptPer1MTokens and completionPer1MTokens');
  });

  describe('offline-fallback', () => {
    it.todo('falls back to static snapshot when APIs unreachable');
    it.todo('serves stale cache on refresh failure');
    it.todo('uses 24h TTL for cache expiration');
  });

  describe('listModels', () => {
    it.todo('filters by provider');
    it.todo('filters by capabilities');
    it.todo('filters by qualityTier');
    it.todo('filters by maxPrice');
    it.todo('excludes deprecated models');
  });

  describe('close', () => {
    it.todo('cancels inflight fetches');
    it.todo('clears cache');
  });
});
```

**2. Create src/selection/strategies/round-robin.test.ts:**

```typescript
import { describe, it } from 'vitest';

describe('RoundRobinStrategy', () => {
  it.todo('distributes requests evenly across 3 keys over 100 requests');
  it.todo('tracks per-provider cycling state independently');
  it.todo('skips ineligible candidates');
  it.todo('resets cycling when available key set changes');
});
```

**3. Create src/selection/strategies/least-used.test.ts:**

```typescript
import { describe, it } from 'vitest';

describe('LeastUsedStrategy', () => {
  it.todo('selects key with highest worst-case remaining percentage');
  it.todo('treats keys with no limits as 100% remaining');
  it.todo('uses config order as tiebreaker when remaining is equal');
  it.todo('skips ineligible candidates');
});
```

**4. Create src/selection/KeySelector.test.ts:**

```typescript
import { describe, it } from 'vitest';

describe('KeySelector', () => {
  describe('per-provider-override', () => {
    it.todo('uses per-provider strategy when configured');
    it.todo('per-request override takes precedence over per-provider');
    it.todo('falls back to global default when no override');
  });

  describe('skip-ineligible', () => {
    it.todo('skips keys in cooldown');
    it.todo('skips keys that exceeded quota');
    it.todo('throws RateLimitError when all keys in cooldown');
    it.todo('throws QuotaExhaustedError when all keys exhausted');
    it.todo('emits provider:exhausted event before throwing');
  });

  describe('custom-strategy', () => {
    it.todo('accepts custom strategy as function');
    it.todo('accepts custom strategy as object with name and selectKey');
    it.todo('falls back to default strategy on custom strategy error');
  });

  describe('single-key-shortcircuit', () => {
    it.todo('skips strategy logic for single-key provider');
    it.todo('still emits key:selected event for single key');
  });

  describe('events', () => {
    it.todo('emits key:selected on every selection');
    it.todo('includes strategy name and reason in event payload');
  });
});
```

  </action>
  <verify>
    <automated>cd /Users/muhammaduzair/Documents/github/experiments/pennyllm/pennyllm && npx tsc --noEmit 2>/dev/null; npm test -- src/catalog/DefaultModelCatalog.test.ts src/selection/strategies/round-robin.test.ts src/selection/strategies/least-used.test.ts src/selection/KeySelector.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>4 test scaffold files exist. All contain it.todo() test cases covering the behaviors listed in VALIDATION.md. Files are parseable by Vitest (no import errors since only vitest is imported). Todo tests show as pending in test output.</done>
</task>

</tasks>

<verification>
- All 4 test files exist and are valid TypeScript
- `npm test -- src/catalog/DefaultModelCatalog.test.ts` runs without error (shows todos)
- `npm test -- src/selection/strategies/round-robin.test.ts` runs without error (shows todos)
- `npm test -- src/selection/strategies/least-used.test.ts` runs without error (shows todos)
- `npm test -- src/selection/KeySelector.test.ts` runs without error (shows todos)
- Test cases map to VALIDATION.md task IDs (CAT-01 through CAT-05, ALGO-01 through ALGO-05)
</verification>

<success_criteria>

1. 4 test scaffold files created at paths matching VALIDATION.md expectations
2. Each test file has it.todo() cases covering the behaviors it will validate
3. Files compile and run under Vitest without errors
4. No actual test implementation (scaffolds only, per CLAUDE.md testing policy)
   </success_criteria>

<output>
After completion, create `.planning/phases/05-model-catalog-selection/05-00-SUMMARY.md`
</output>
